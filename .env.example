# 데이터베이스 연결 문자열 (필수)
# PostgreSQL 데이터베이스 URL
# 예시: postgresql://username:password@host:port/database
DATABASE_URL=postgresql://user:password@localhost:5432/kcoat_db

# 세션 암호화 키 (필수)
# 랜덤한 문자열을 생성해서 사용하세요 (최소 32자)
SESSION_SECRET=your-secret-key-here-change-this-in-production

# API URL (Replit 배포시 필수)
# 백엔드 서버 URL을 지정합니다
# 로컬 개발: http://localhost:3001
# Replit 배포: https://your-replit-username-kcoat.replit.app
VITE_API_URL=http://localhost:3001

# CORS 허용 오리진 (선택)
# 쉼표로 구분하여 여러 origin 지정 가능
# 예시: https://kcoat.replit.app,https://kcoat.com
ALLOWED_ORIGINS=http://localhost:5000,http://localhost:3000

# 서버 포트 (선택, 기본값: 3001)
PORT=3001

# 환경 (production 또는 development)
NODE_ENV=development

# n8n 웹훅 URL (선택, AI 생성 기능용)
# constants.ts 파일에서 WEBHOOK_URL로 사용
VITE_WEBHOOK_URL=https://your-n8n-webhook-url

# 이메일 발송 설정 (비밀번호 재설정용, 향후 구현시 사용)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-password
